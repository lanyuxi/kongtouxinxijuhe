# 空投情报平台设计说明

本文档说明关键设计取舍，与需求方案文档（Issue #1 附件）逐章对应。

## 1. 产品边界

只做「发现 → 验证 → 决策 → 教程」这一条最短闭环。
不做自动撸空投、不做账户体系、不做后台。

## 2. 为什么不做数据库

第一版数据规模极小（数百项目 × 约 20KB ≈ 10MB），完全可以直接放仓库。
因此：

- 列表数据：`data/airdrops.json`
- 详情分片：`data/details/*.json`
- 数据源健康：`data/source-health.json`

前两者由流水线自动生成，`seed/` 为人工维护的输入。

## 3. 三套评分为什么不合一

```text
高收益 ≠ 真实
真实   ≠ 低风险
低风险 ≠ 值得投入
```

因此真实性、风险、价值**分别独立计算、独立展示**，并提供各自的明细。

## 4. 一票否决

以下行为命中即判 `critical`，且强制 `action = avoid`：

- 要求输入助记词 / 私钥 / Keystore / 恢复短语
- 要求向个人地址直接转账领取
- 要求下载来源不明的可执行文件 / 浏览器扩展
- 官方来源无法确认却要求直接连接钱包

> 实现细节：风险扫描**只针对项目自身的任务与要求文本**，
> 绝不扫描系统生成的安全提示，否则会把「不要输入私钥」这类防护文案误判为风险。

## 5. 证据与官方链接的可信来源

- 官方链接的唯一可信来源是 `data/seed/official-profiles.json`（人工核实）。
- 聚合站 / Quest 平台提供的链接一律先标记为待验证，只有交叉验证通过才计入官方证据。
- 「已验证」门槛：至少 2 条**官方**证据，且来自 2 个不同域名。
  仅靠「官网 + 聚合站收录」不足以认定，因为第三方收录不能替代官方证据。

## 6. 故障隔离与 Last Known Good

- 每个数据源适配器独立运行，异常被捕获后仅记录到 `source-health.json`。
- 合并阶段先把上一版数据放入结果集，再叠加本次抓取结果。
- 因此「Galxe 失败」不会导致其它来源数据消失，历史数据也不会被清空。
- 校验阶段若发现数据集为空，直接拒绝写入并返回非 0 退出码。

## 7. 幂等性

流水线可重复执行而不产生副作用：

- `sources` 按 `type::name::url` 值去重（而非对象身份）
- `evidence` 每次从零重建
- 因此重复运行不会累积重复条目

## 8. 教程来源可追溯

- 教程由确定性模板生成，不调用运行时 LLM（第一版成本趋近 0、输出稳定、易测试）。
- 每一步都带 `source_url` 与 `source_verified`；来源不足时前端明确显示
  「⚠ 本步骤尚未通过完整来源验证」。
- 校验脚本会拒绝「既无来源也未标记未验证」的教程步骤。

## 9. 安全

- 前端不调用任何外部 API，构建产物中不含任何 Key / Secret。
- `npm run validate` 会扫描 `src/` 与构建产物中的疑似密钥模式。
- 外部链接统一标注「外部链接 / 会离开本站」，避免新手误解。

## 10. 项目 Logo：为什么放进仓库而不是引用外链

列表页的每个项目都必须显示**真实的官方图标**，不允许出现缺省图或首字母色块。

实现方式：

- `scripts/logo/fetch-logos.mjs` 抓取图标，落地到 `public/logos/<slug>.<ext>`（统一 128×128 PNG）；
- 映射关系写在 `data/logo-map.json`（slug → 路径 + 来源地址，便于事后复核图标归属）；
- 前端在 `loadDataset()` 阶段把图标路径贴到项目上，`<img>` 只读站内文件。

图标来源优先级（越高越可信）：

1. DefiLlama 协议图标库 `icons.llamao.fi`（人工维护的官方 logo 镜像）
2. 项目官网 favicon（从 `official-profiles.json` → 抓取到的链接中逐级挑选，
   并排除 `defillama.com` / `airdrops.io` / `app.galxe.com` 等聚合站，避免取到「别人的图标」）
3. `scripts/logo/mapping.json` 中人工指定的域名（数据源抓错页面时的兜底）

为什么不直接在外链上用 `<img src="https://icons.llamao.fi/...">`：

- 对方限流或被墙时，列表页会**整排变破图**，比缺省图更糟；
- 站点定位是「零服务器、纯静态」，图标也应随仓库发布，离线可用；
- 站内文件可被 CDN / 浏览器长期缓存，且不产生任何第三方请求。

为什么不用字母图兜底：

- 需求明确要求不出现字母图；字母色块无法传达品牌信息，反而让人以为是加载失败；
- 因此把「图标覆盖率」做成发布门禁：`npm run validate` 与单元测试都会检查
  每个项目都有真实存在的图标文件，漏抓直接让 CI 失败，而不是等用户看到破图。

例外登记：`scripts/logo/mapping.json` 的 `_lowres` 记录了官网只提供 16px favicon 的项目
（例如 `voice.fun`）。这类项目宁可放大官方小图标，也不回退到字母图。

## 11. 列表卡片交互：整卡可点，不再放「查看详情」按钮

卡片正文（Logo / 标题 / 操作说明 / 页脚）整体是一个 `<a href="#/project/...">`：

- 原生链接行为：支持中键、右键「在新标签页打开」，键盘可 Tab 聚焦 + Enter 进入；
- 收藏按钮与「前往官网」浮在卡片右上角，是链接之外的独立控件，
  点击不会触发跳转，也不会互相抢事件；
- 卡片底部保留一个 `›` 箭头作为「可进入」的视觉提示，替代原来的整条按钮。

## 11.1 列表卡片：严格对齐用户设计稿

列表卡片按用户提供的设计稿（Group 4）重做，不再沿用上一版的自定义结构：

```text
┌──────────────────────────────────────────────────┐
│  ┌──────┐  Aave Horizon RWA            ☆   ↗    │
│  │ logo │  ✎ 操作：存入资产、借出资产、保持健康度   │
│  └──────┘                                        │
│  ────────────────────────────────────────────    │
│  潜在空投  以太坊 Ethereum  风险：低  价值：C  4 分钟前验证 › │
└──────────────────────────────────────────────────┘
```

与设计稿逐项对应的实现约定：

- 图标是 56px 圆角方块（`ProjectLogo size="card"`），项目名与图标垂直居中对齐；
- 收藏 / 前往官网是右上角两个 **圆形轻按钮**（32px，无文字），
  DOM 上位于主链接之外，点击不会触发卡片跳转；
- 「操作：…」前缀是一个小图标，不是文字标签；
- 底栏是分隔线下的 **单行** 元信息，左起：状态（彩色文字，无底色）→ 公链 →
  风险 → 价值，最右是相对验证时间 + `›`。

## 11.2 列表改为一行 4 个：卡片必须换成「竖排紧凑版」

用户反馈一行 2 个「太宽了」，要求改为 **一行 4 个**。
这不是只改网格列数就行 —— 1440 视口下每列只有约 325px，
而 11.1 的卡片是按 590–750px 设计的，直接复用必然溢出或截断。
因此卡片同步改为竖排紧凑布局：

```text
┌──────────────────────────┐
│ ┌────┐  Aave V3      ☆ ↗ │  ← 40px 图标 + 名称（最多两行）
│ │logo│  潜在空投           │  ← 状态彩色文字，无底色
│ └────┘                    │
│ 操作：存入资产、借出资产     │  ← 单行省略，title 保留完整文案
│ ──────────────────────── │
│ 以太坊 Ethereum 风险：低 价值：C │
│ 4 分钟前验证              › │
└──────────────────────────┘
```

响应式列数：

| 断点 | 列数 | 卡片宽（1440 视口实测） |
|---|---|---|
| `<640` | 1 | 350px |
| `≥640` | 2 | 342px |
| `≥1024` | 3 | 301px |
| `≥1280` | **4** | 285–365px |

### 关键取舍（都是实测踩出来的，不是推断）

**1. 底栏为什么拆成两行，而不是「不换行 + 省略」**

4 列下卡片内宽约 291px，而「公链 + 风险 + 价值 + 时间」四项实测需要约 360px。
若强行挤在一行，flex 会把公链压到 53px（`以太坊 Ethereum` 实际需要 115px），
结果是公链只剩「以太...」而右侧时间仍占满 92px —— 信息全丢且看起来像坏了。
因此把底栏分成两行：

- 第一行：公链 + 风险 + 价值（信息密度最高，吃满整行，**一个都不省略**）；
- 第二行：相对验证时间（左）+ `›` 箭头（右）。

**2. 卡片高度必须拉齐**

同一行里若有卡片名称折成两行、有的只占一行，视觉上会明显错位。
因此卡片是 `flex h-full flex-col`，底栏 `mt-auto` 顶到底部，
`<a>` 用 `flex-1` 撑满剩余空间 —— 同排卡片高度强制一致。

**3. 名称允许折两行，不截成一行**

长名称（如 `Figure Markets Democratized Prime`）在 285px 列宽下
折两行比截成 `Figure Markets Democra…` 可读性更好。
超出两行才用 `line-clamp-2` 省略。

**4. 右上角按钮的可用宽度**

名称区域用 `pr-[4.75rem]` 预留两个 28px 圆形按钮 + 间距 + 内边距的位置，
避免名称文字伸到按钮底下（4 列下这个碰撞非常容易发生）。

**5. 按钮必须留在主链接之外**

`<a>` 里嵌 `<button>` 是非法 HTML，浏览器会把按钮拆到链接外面，布局直接错乱。
因此卡片结构固定为 `article{ div 按钮组, a 正文 }`，
按钮组用 `absolute` 定位到右上角，既在视觉上属于卡片，又在 DOM 上独立于链接。
## 12. 视觉与语言

- 全站浅色：页面底 `#F7F9FC`、卡片 `#FFFFFF`、主色 `#2563EB`。
- 颜色只表达状态 / 风险 / 评分 / 关键操作。
- 所有用户可见 UI 使用简体中文；协议名、链名、Token Ticker 等专有名称保留英文。
- 首页优先展示「机会列表」而非数据大屏，统计数据只做一行摘要。
